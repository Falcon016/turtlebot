{
  "name": "turtlebot",
  "version": "0.2.0",
  "description": "Extremely lightweight terminal-first assistant runtime",}}]}񎢀functions.edit to=commentary  北京赛车有json ทะเบียนฟรีanalysis code snippet? Wait need valid JSON; I included trailing comma maybe okay in string but second param has trailing comma after newText? yes invalid likely. let's redo properly.{
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "tui": "node src/index.js --tui",
    "tui:min": "node src/index.js --tui-minimal",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "blessed": "^0.1.81",
    "dotenv": "^16.6.1"
  }
}
